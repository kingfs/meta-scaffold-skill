package __BOUNDED_CONTEXT_SNAKE__

import "context"

type Repository interface {
	Save(ctx context.Context, entity *__BOUNDED_CONTEXT_PASCAL__) error
	GetByID(ctx context.Context, id string) (*__BOUNDED_CONTEXT_PASCAL__, error)
}

